#Deploy script for robots after the 2016 update
ssh admin@roboRIO-1425.lan rm /home/lvuser/FRCUserProgram
# if deploy script is run on linux, rsync can be used and a backup can be created
#if [ $(uname -s) = Linux ]
#then
#	echo "We're using Linux, so we can copy using rsync!"
#	rsync -bvpE FRCUserProgram admin@roboRIO-1425.lan:/home/lvuser/FRCUserProgram
#else
	echo "We're not using Linux, so scp will be used"
	scp FRCUserProgram admin@roboRIO-1425.lan:/home/lvuser/FRCUserProgram
#fi
ssh admin@roboRIO-1425.lan ". /etc/profile.d/natinst-path.sh; chmod a+x /home/lvuser/FRCUserProgram; /usr/lan/frc/bin/frcKillRobot.sh -t -r"
